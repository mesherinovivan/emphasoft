[pytest]
addopts = --reuse-db
DJANGO_SETTINGS_MODULE = emphasoft.src.api.settings
testpaths = ../tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integrated: marks tests as integrated (deselect with '-m "not integrated"')
